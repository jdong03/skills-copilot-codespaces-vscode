function calculateNumbers(var1, var2) {
  return Math.round(var1) + Math.round(var2);
}

function differenceNumber(var1, var2) {
  return Math.round(var1) - Math.round(var2);
}

function productNumber(var1, var2) {
  return Math.round(var1) * Math.round(var2);
}